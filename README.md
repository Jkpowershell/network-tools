# network-tools
I want every terminal to have the same diagnostic toolset, versions, shell config, and updates.

run these commands

git clone git@gitlab.company.com:noc/noc-workstation.git
cd noc-workstation
./install.sh
